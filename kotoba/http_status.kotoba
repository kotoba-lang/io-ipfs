(ns kotoba.lang.ipfs.http-status
  (:require [kotoba.lang.ipfs.status-bounds :as bounds])
  (:export [http-ok]))

(defn http-ok [status]
  (if (and (>= status (bounds/lower))
           (<= status (bounds/upper)))
    1
    0))
