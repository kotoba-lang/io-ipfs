;; Hand-port backlog from bb.edn (ADR-2607173000).
{:test {:kind :complex, :doc "Run kotoba.lang.ipfs tests (pure helpers + JVM orchestration; no network).", :form "(do (require 'clojure.test 'kotoba.lang.ipfs-test) (let [r (clojure.test/run-tests 'kotoba.lang.ipfs-test)] (when (pos? (+ (:fail r) (:error r))) (System/exit 1))))"}}
